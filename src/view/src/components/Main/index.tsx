import { FC } from 'react'
import { Editor } from '../Editor'

import { Container } from './styles'

export const Main: FC = () => {
  return (
    <Container>
      <Editor />
    </Container>
  )
}
